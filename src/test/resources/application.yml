spring:
  datasource:
    driver-class-name: org.h2.Driver
    username: sa
    url: jdbc:h2:mem:testdb
    password: ''
  data:
    redis:
      port: '6379'
      host: localhost
  application:
    name: Preonboarding-Backend-Course
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    properties:
      hibernate:
        format_sql: 'true'
    hibernate:
      ddl-auto: create-drop
    show-sql: 'true'
jwt:
  secret:
    key: adlkfjalwekrjoeiwrqlekrjaskjfasdfk
